import * as ToDoActions from '../actions/todo.actions';
import TodoState from '../state/AppState';

export const todoreducer = (state: TodoState, action: ToDoActions.ToDoActionType ) => {
    switch(action.type){
        case 
    }
};